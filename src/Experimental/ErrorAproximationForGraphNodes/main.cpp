//This example shoud prove that The local optimisation I developed in this work
//actually leads to an improvement of the Nodes after several iterations
//The algorythm is just scetches the methode so it's basically c-Style coded
//Note that:
//In this example also the Graph layout is already correct !!!

#include <iostream>
#include <random>

int main(int argc, char *argv[]){
	
  int samples = 0;
  int maxSamples = 128;
  
  float ErrNode0 = 0.0;
  float ErrNode1 = 0.0;
  
  
  
  while(samples < maxSamples){
    
    
    samples++;
  }
	
}